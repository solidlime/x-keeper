# ─────────────────────────────────────────────────────────────────────────────
# keep-image-saver 設定ファイルのテンプレート
# このファイルをコピーして .env という名前で保存し、実際の値を入力してください。
#   cp .env.example .env
# ─────────────────────────────────────────────────────────────────────────────

# ── Google Keep ──────────────────────────────────────────────────────────────

# Google アカウントのメールアドレス
GOOGLE_EMAIL=your-email@gmail.com

# Google OAuth2 認証情報 (python -m src.token_setup で自動取得・保存される)
# Google Cloud Console でプロジェクトを作成し OAuth2 クライアント ID を発行してください
# 詳細: https://console.cloud.google.com/apis/credentials

# client_secrets.json のパス (省略時はプロジェクトルートの client_secrets.json を使用)
# 任意の場所に配置する場合に設定する (絶対パスまたは実行ディレクトリからの相対パス)
# GOOGLE_CLIENT_SECRETS_FILE=/path/to/client_secrets.json

# GOOGLE_OAUTH_CLIENT_ID=123456789-xxx.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_SECRET=GOCSPX-xxx
# GOOGLE_OAUTH_REFRESH_TOKEN=1//xxx  (token_setup 実行後に自動入力)

# ── X (Twitter) / gallery-dl ─────────────────────────────────────────────────

# (推奨) gallery-dl に渡すブラウザ Cookie ファイルのパス (Netscape 形式)
# 設定するとスレッド全体 (返信ツイートを含む) の画像を一括取得できる。
# ブラウザ拡張機能 "Get cookies.txt LOCALLY" 等で x.com のクッキーを書き出し
# data/ フォルダに配置すること。
# 不要ならコメントアウトのままでよい。
# GALLERY_DL_COOKIES_FILE=./data/x.com_cookies.txt

# ── 保存先 ───────────────────────────────────────────────────────────────────

# 画像の保存先ルートディレクトリ (Docker ボリュームのマウントポイント)
# docker-compose.yml のボリューム設定と合わせること
SAVE_PATH=/data/images

# ── ポーリング ────────────────────────────────────────────────────────────────

# Google Keep の確認間隔 (秒)。デフォルト: 60
POLL_INTERVAL_SECONDS=60

# ── Web セットアップサーバ ────────────────────────────────────────────────────

# Web セットアップ画面のポート番号 (ブラウザで http://localhost:<このポート> を開く)
# docker-compose.yml のポートマッピングと自動的に連動する
WEB_SETUP_PORT=8989

# ── ロギング ──────────────────────────────────────────────────────────────────

# ログレベル: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO
